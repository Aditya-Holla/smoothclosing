# Credentials & tokens (NEVER commit these)
.env
credentials.json
token.json
gbp_token.json
*.pem
*.key

# Pipeline outputs (generated on each run)
leads.csv
leads_old.csv
leads_with_equity.csv
leads_traced.csv
leads_sms_sent.csv
leads_tmp_trace.csv
skip_genie_results.csv
travis_foreclosures.csv
# New-batch outputs (only the leads from the latest pipeline run)
leads_new.csv
leads_new_equity.csv
leads_new_traced.csv
leads_new_sms_sent.csv
# SMS history — contains real phone numbers, never commit
sms_history.csv

# State (tracked separately — committed to keep incremental processing)
# pipeline_state.json  ← intentionally NOT ignored

# Downloaded PDFs (too large for git)
input_pdfs/

# Logs
*.log
automation.log
.last_run_date

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.venv-agents/
venv/
*.egg-info/

# Playwright browser cache
.skipgenie_session/
.cad_session/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
